# OpenSaC for agents

The maintained agent instructions live in [the Search as Code skill](../skills/search-as-code/SKILL.md).
The skill covers direct Python search, fetch, batches and local result composition. It is a standalone
repository resource, not part of the Python package.
