{
  "mcpServers": {
    "opensac": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "opensac", "mcp"],
      "env": {
        "SAC_API_BASE": "${SAC_API_BASE:-http://127.0.0.1:8000}",
        "SAC_API_KEY": "${SAC_API_KEY:-}"
      }
    }
  }
}
