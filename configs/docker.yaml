# Docker/Compose profile. Replace the storage path with the same absolute host
# path used by OPENSAC_CONTAINER_DATA_DIR in compose.env.

api:
  host: 0.0.0.0
  port: 8000

storage:
  data_dir: /absolute/path/to/opensac-data
  # Keep the socket in a dedicated directory. Docker Desktop mounts this
  # directory into sandboxes without exposing session data.
  broker_socket: /absolute/path/to/opensac-data/broker/broker.sock

backends:
  search:
    provider: serper
  document:
    provider: jina
  rerank:
    provider: lexical

sandbox:
  image: ghcr.io/liuqi6777/opensac-sandbox:0.8.5
  # Change to darwin when the service talks to Docker Desktop for macOS.
  docker_host_platform: linux
  mode: cold
